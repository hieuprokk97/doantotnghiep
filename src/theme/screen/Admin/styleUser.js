import { View, Text, StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1, 
        padding: 20, 
    },
    title: {
        fontSize: 19,
        fontWeight: 'bold'
    }
})

export default styles